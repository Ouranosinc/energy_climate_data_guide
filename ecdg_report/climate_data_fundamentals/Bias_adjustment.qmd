---
title: Downscaling and Bias Adjustment
---


Applications in electricity system planning and operations are typically sensitive to fine-scale variations of climate parameters. Thus, it is essential that climate inputs reflect the regional or local meteorological conditions with sufficient detail. GCMs operate at relatively coarse spatial resolutions (typically 100-250 km) and therefore their outputs are often inadequate for regional or local scale assessment. To address this, two categories of downscaling techniques are applied: statistical downscaling and dynamical downscaling.

Statistical downscaling establishes empirical relationships between large-scale GCM variables and local climate observations, using historical data as a training base. Available methods are computationally efficient and have been widely used. In contrast, dynamical downscaling refers to the higher-resolution [Regional Climate Models (RCMs)](../datasets/climate_projection_data/Climate_Projection_Datasets.qmd) which are driven by GCM output at their boundaries to explicitly simulate regional climate processes. 

Climate model output tends to deviate systematically from observed climatology. The biases may relate to the number of rainfall days or precipitation amount, or a model may be consistently too high or too low. Spatial resolution and process parameterization, among others, are involved in causing these deviations. **Bias adjustment**, also commonly called bias correction, uses observations or another reference dataset to reduce selected systematic biases in climate-model simulations. It is particularly relevant when climate data are used to derive threshold-based climate indicators or to drive impact models calibrated to historical observations, whose response may be unrealistic when driven with unadjusted simulated climate data.

## Delta-Change Method

The delta-change method is a simple way to combine a modelled climate-change signal with an observed historical time series. The change between historical and future periods in a climate-model simulation is calculated as a difference or ratio and then applied to observations. Additive changes are commonly used for temperature, while multiplicative factors are often used for precipitation to avoid negative values.

This method is transparent, has relatively low data requirements, and preserves the observed temporal sequence while imposing the modelled mean change. However, it assumes that model bias remains stationary and that changes in the mean adequately represent changes in variability and extremes. It is therefore not a substitute for more comprehensive bias adjustment when an application is sensitive to distributions, sequencing, or extreme events.

Different approaches to bias adjustment and statistical downscaling have been developed. They include univariate and multivariate approaches. A recent review of available methods can be found in @Menapace2025, and an in-depth discussion of statistical downscaling and bias adjustment for climate research is available in @MaraunWidmann2018. The currently most widely accepted approach to bias adjustment consists of some variant of quantile mapping. Bias-adjustment methods make assumptions that include stationarity of model bias, which allows adjustment factors developed for historical periods to be applied to future simulations, and stability of the spatial patterns transposed in the adjustment process. While these methods substantially improve some statistical properties of climate-model output, the resulting data may still underestimate extreme events and may not adjust their frequency.

RCM outputs are subject to biases similar to those in GCM output, so the same bias-adjustment methods may be applied. Because an RCM already operates at regional resolution, bias adjustment does not necessarily change its grid spacing.

Most datasets recommended in this guide have undergone bias adjustment, including [ESPO-G6](../datasets/climate_projection_data/ESPO-G6.ipynb), [CanDCS-M6](../datasets/climate_projection_data/CanDCS-M6.ipynb), [CanLEAD](../datasets/climate_projection_data/CanLEAD.ipynb) and [NEX-GDDP-CMIP6](../datasets/climate_projection_data/NEX-GDDP-CMIP6.ipynb). See [Climate Projection Datasets](../datasets/climate_projection_data/Climate_Projection_Datasets.qmd) for the characteristics of the documented products. Open-source Python code for applying different bias-adjustment methods is available in the [xsdba library](https://xsdba.readthedocs.io/en/latest/index.html).

## References

::: {#refs}
:::
